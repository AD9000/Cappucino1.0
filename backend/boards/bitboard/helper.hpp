#include <cstdint>
#include "../masks.hpp"
using namespace masks;

int findLDiag(bitboard);
int findRDiag(bitboard);
bitboard generateBishopMask(uint8_t, uint8_t);
bitboard generateRookMask(uint8_t, uint8_t);
