#include <map>
using namespace std;

class board
{
private:
    // List of pieces on the board
    map<uint8_t, uint8_t> pieces;
public:
    board(/* args */);
    ~board();
};

board::board(/* args */)
{
    pieces.add
}

board::~board()
{
}
